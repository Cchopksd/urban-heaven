import Navbar from "../components/Navbar";

const Dash = () => {

    return (
        <div>
            <Navbar />

            <p>Dashboard</p>
            {/* <p>{`data: ${userData.username}`}</p> */}
            <p></p>
        </div>
    );
};

export default Dash;
